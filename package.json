{
  "name": "villahunter",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/core.js && node --check src/search-model.js && node --check api/search.js && node --check api/search-v2.js && node --check api/search-v3.js && node --check api/health.js",
    "serve": "python -m http.server 8000",
    "dev": "vercel dev"
  },
  "engines": {
    "node": ">=20"
  }
}
